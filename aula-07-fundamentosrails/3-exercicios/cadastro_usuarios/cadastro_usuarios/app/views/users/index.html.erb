<h1>Usuários cadastrados</h1>

<% if @users.empty? %>
  <p>Nenhum usuário cadastrado ainda.</p>
<% else %>
  <ul>
    <% @users.each do |user| %>
      <li><strong><%= user[:nome] %></strong> — <%= user[:email] %></li>
    <% end %>
  </ul>
<% end %>

<a href="/creation">Cadastrar novo</a>
